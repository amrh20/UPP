import { MaximumInputSizeDirective } from './maximum-input-size.directive';

describe('MaximumInputSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MaximumInputSizeDirective();
    expect(directive).toBeTruthy();
  });
});
